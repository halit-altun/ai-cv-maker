# Used by `npm run dev` (Next.js development)
NEXT_PUBLIC_API_URL=http://localhost:3011
# Backend toplu PDF render — Backend INTERNAL_PIPELINE_SECRET ile aynı olmalı
INTERNAL_PIPELINE_SECRET=change-me-pipeline-secret
